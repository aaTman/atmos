[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atmos"
version = "0.1.0"
description = "Atmospheric science calculations for Python"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[tool.hatch.build.targets.wheel]
packages = ["atmos"]
